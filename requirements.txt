streamlit>=1.32
folium>=0.15.0
requests>=2.31.0
google-genai>=1.0.0

# 도로망 경로/소요시간 계산(선택). 설치가 실패하거나 로딩이 안 되면
# 앱이 자동으로 '직선거리' 기준으로 폴백합니다. 가벼운 배포를 원하면
# 아래 두 줄을 지우세요(그럴 경우 항상 직선거리 기준으로 동작).
osmnx>=1.9
networkx>=3.0
