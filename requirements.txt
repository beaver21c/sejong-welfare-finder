streamlit>=1.32
folium>=0.15.0
requests>=2.31.0
google-genai>=1.0.0

# 도로망 기반 '차량 소요시간'(선택 기능).
# 기본은 꺼져 있어 앱은 즉시 뜨고, 사이드바에서 사용자가 켤 때만 도로망을 불러옵니다
# (최초 1~3분 다운로드, sejong_drive.graphml 을 커밋해두면 수 초). 없거나 실패하면
# 직선거리로 자동 폴백합니다.
osmnx>=1.9
networkx>=3.0
