streamlit>=1.32
folium>=0.15.0
requests>=2.31.0
google-genai>=1.0.0

# --- (선택) 도로망 기반 소요시간 ---
# 아래 두 줄의 주석을 풀면 OSMnx 도로망으로 '차량 소요시간'을 계산합니다.
# 다만 무료 호스팅(Streamlit Community Cloud 등)에서는 딸려오는 무거운
# 지리 패키지 때문에 빌드가 느리거나 메모리 부족으로 앱이 '계속 로딩'
# 상태에 빠질 수 있어 기본은 꺼둡니다. (없으면 '직선거리'로 자동 동작)
# 활성화 시 packages.txt에 libspatialindex-dev 를 추가하세요.
# osmnx>=1.9
# networkx>=3.0
