streamlit>=1.32
folium>=0.15.0
requests>=2.31.0
google-genai>=1.0.0

# 도로망 기반 경로/소요시간 (지도에 경로선 표시).
# scipy / scikit-learn 은 osmnx 의 nearest_nodes(최근접 도로 노드 탐색)에 필요합니다.
# 이 두 줄이 빠지면 도로망은 로딩돼도 경로 계산이 실패해 경로선이 안 그려집니다.
osmnx>=1.9
networkx>=3.0
scipy>=1.11.0
scikit-learn>=1.3.0
